@page
@model MealPlan.Pages.Biodata.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.BioData[0].Weight)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.BioData[0].Height)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.BioData[0].Gender)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.BioData[0].Birthday)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.BioData[0].Person)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.BioData) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Weight)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Height)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Gender)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Birthday)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Person.Firstname)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.BioId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.BioId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.BioId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
